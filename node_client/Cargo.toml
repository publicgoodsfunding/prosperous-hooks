[package]
name = "prosperous-client-native"
version = "0.1.0"
edition = "2021"

[lib]
name = "prosperous_client_native"
crate-type = ["staticlib"]

[dependencies]
client     = { path = "../client" }
tokio      = { version = "1", features = ["rt", "rt-multi-thread"] }
serde_json = "1"
